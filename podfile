# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'AltAutoLayout' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for AltAutoLayout

  pod 'Texture'
  target 'AltAutoLayoutTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'AltAutoLayoutUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
